# Supabase project settings (Settings -> API in the Supabase dashboard)
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Edge Function secrets (set via `supabase secrets set`, never exposed to the frontend)
# SUPABASE_SERVICE_ROLE_KEY=service-role-key-here
# STUDENT_ID_HMAC_SECRET=a-long-random-secret-used-to-hash-student-identifiers
